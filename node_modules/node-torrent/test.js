var Client = require('./lib/client');
var client = new Client({logLevel: 'INFO'});
var torrent = client.addTorrent('test.torrent');
torrent.on('complete', function() {console.log('complete!');});

for (var k in client.torrents) {t = client.torrents[k];}

